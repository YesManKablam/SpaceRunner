using UnityEngine;
using System.Collections;

public class ResLock : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Screen.SetResolution (600, 900, false);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
